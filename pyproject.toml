[project]
name = "win-health-dashboard"
version = "0.1.0"
description = "Lightweight local system health checker for Windows endpoints."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111",
  "uvicorn>=0.30",
  "psutil>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
